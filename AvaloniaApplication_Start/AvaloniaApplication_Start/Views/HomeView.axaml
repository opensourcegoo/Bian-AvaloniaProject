<UserControl xmlns="https://github.com/avaloniaui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
			 x:Class="AvaloniaApplication_Start.Views.HomeView">
	<Grid Background="DarkSalmon">
		<StackPanel>
			<TextBlock Text="{Binding  MainName}" />
			<!--<Button Content="124321" Command="{Binding SubmitCommand}" />-->
			<Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" />
			<Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" />
			<Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" /><Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" />
			<Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" /><Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" />
			<Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" /><Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" />
			<Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" /><Button Content="this is" Command="{Binding SubmitCommand}" Background="DarkSalmon" />
		</StackPanel>
		<StackPanel  Height="200" Width="300">
			
			<Button Content="this is" Command="{Binding SubmitCommand}" />
			<Button Content="this is" Command="{Binding SubmitCommand}" />
			
		</StackPanel>

	</Grid>
</UserControl>
