<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="clr-namespace:AvaloniaApplication_Start.ViewModels"
			 xmlns:local="clr-namespace:AvaloniaApplication_Start.Views"
			 xmlns:i="using:Avalonia.Xaml.Interactivity"
			 xmlns:ia="using:Avalonia.Xaml.Interactions.Core"
			 
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
			 x:DataType="vm:BasicControlViewModel"
             x:Class="AvaloniaApplication_Start.Views.BasicControlView">
	
	<UserControl.DataTemplates>
		<DataTemplate DataType="{x:Type vm:HomeViewModel}">
			<local:HomeView />
		</DataTemplate>
		<DataTemplate DataType="{x:Type vm:AboutViewModel}">
			<local:AboutView />
		</DataTemplate>
	</UserControl.DataTemplates>


	<!--xmlns:b="using:Avalonia.Interactivity"
	xmlns:behaviors="using:Xaml.Behaviors"
	xmlns:i="using:Avalonia.Xaml.Interactivity"-->

	<DockPanel Background="Red" HorizontalAlignment="Stretch"  LastChildFill="True">
		<Border DockPanel.Dock="Left"
			  Width="200"
			  Background="#23272A">

			<ListBox Background="Transparent" BorderThickness="0" Margin="0" Padding="0" SelectionMode="Single">
				<!--这是第一种写法 avalonia推荐-->
				<i:Interaction.Behaviors>
					<EventTriggerBehavior EventName="SelectionChanged">
						<InvokeCommandAction Command="{Binding $parent[UserControl].DataContext.TestLoadCommand}"
                                             CommandParameter="{Binding $self.SelectedItem}" />
					</EventTriggerBehavior>
				</i:Interaction.Behaviors>

				<!--这是第二种写法 WPF风格-->
				<!--<i:Interaction.Behaviors>
					<ia:EventTriggerBehavior EventName="SelectionChanged">
						<ia:InvokeCommandAction
							Command="{Binding TestLoadCommand, RelativeSource={RelativeSource  AncestorType=UserControl,Mode=DataContext}}"
							CommandParameter="{Binding $self.SelectedItem}" />
						-->
				<!--都可以使用-->

				<!--
							-->
				<!--Command="{Binding TestLoadCommand, RelativeSource={RelativeSource  AncestorType=UserControl,Mode=DataContext}}"-->
				<!--
							-->
				<!--CommandParameter="{Binding SelectedItem,RelativeSource={RelativeSource AncestorType=ListBox,Mode=FindAncestor}}" />-->
				<!--
							-->
				<!--CommandParameter="{Binding $self.SelectedItem}" />-->
				<!--
					</ia:EventTriggerBehavior>
				</i:Interaction.Behaviors>-->


				<!--<ListBox.ItemTemplate>
					<i:Interaction.Behaviors>
						<EventTriggerBehavior EventName="SelectionChanged">
							<InvokeCommandAction
								Command="{Binding TestLoadCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
								CommandParameter="{Binding $self.SelectedItem}" />
						</EventTriggerBehavior>
					</i:Interaction.Behaviors>
				</ListBox.ItemTemplate>-->

				<!--<ListBox.ItemTemplate>
					<DataTemplate>
						<Border Background="Red">
							<i:Interaction.Behaviors>
								<EventTriggerBehavior EventName="PointerPressed">
									<InvokeCommandAction
										Command="{Binding TestLoadCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
										CommandParameter="{Binding .}" />
								</EventTriggerBehavior>
							</i:Interaction.Behaviors>
							<StackPanel Orientation="Horizontal" Spacing="10" Margin="10">
								<Path Data="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"
									  Fill="White" Width="16" Height="16" VerticalAlignment="Center" Margin="0,-10,0,0"/>
								<TextBlock Text="{Binding Name}" Foreground="White" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
							</StackPanel>
						</Border>
					</DataTemplate>
				</ListBox.ItemTemplate>-->

				<ListBoxItem>
					<StackPanel Orientation="Horizontal" Spacing="10" Margin="10">
						<Path Data="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"
							  Fill="White" Width="16" Height="16" VerticalAlignment="Center" Margin="0,-10,0,0"/>
						<TextBlock Text="Home" Foreground="White" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
					</StackPanel>
				</ListBoxItem>
				<ListBoxItem>
					<StackPanel Orientation="Horizontal" Spacing="10" Margin="10">
						<Path Data="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"
							  Fill="White" Width="16" Height="16" VerticalAlignment="Center" Margin="0,-10,0,0"/>
						<TextBlock Text="About" Foreground="White" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
					</StackPanel>
				</ListBoxItem>
				<ListBoxItem>
					<StackPanel Orientation="Horizontal" Spacing="10" Margin="10">
						<Path Data="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"
							  Fill="White" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,0,0"/>
						<TextBlock Text="Default" Foreground="White" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
					</StackPanel>
				</ListBoxItem>
				<ListBoxItem>
					<StackPanel Orientation="Horizontal" Spacing="10" Margin="10">
						<Path Data="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"
							  Fill="White" Width="16" Height="16" VerticalAlignment="Center" Margin="0,-10,0,0"/>
						<TextBlock Text="首页" Foreground="White" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
					</StackPanel>
				</ListBoxItem>
				<ListBoxItem>
					<StackPanel Orientation="Horizontal" Spacing="10" Margin="10">
						<Path Data="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"
							  Fill="White" Width="16" Height="16" VerticalAlignment="Center" Margin="0,-10,0,0"/>
						<TextBlock Text="首页" Foreground="White" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
					</StackPanel>
				</ListBoxItem>
			</ListBox>
		</Border>
		<ContentControl Background="#2F3136" Content="{Binding CurrentViewModel2}"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
			<!--<ContentControl.Content>
				<local:DefaultView />
			</ContentControl.Content>-->
		</ContentControl>
	</DockPanel>
</UserControl>
